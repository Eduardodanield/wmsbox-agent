# Manipulação do CSV de estoque em memória
pandas>=2.2.0

# Lê a chave de API do arquivo .env sem precisar exportar variável de ambiente no sistema
python-dotenv>=1.0.0

# Núcleo do LangChain (orquestração do agente)
langchain>=1.0.2

# create_pandas_dataframe_agent mora aqui, fora do pacote principal do langchain
langchain-experimental>=0.3.4

# Cliente compatível com a API da OpenAI - usado aqui para falar com o
# endpoint roteador da Hugging Face (router.huggingface.co/v1)
langchain-openai>=1.0.0

# create_pandas_dataframe_agent usa isso por baixo dos panos para formatar
# uma amostra da tabela (df.head().to_markdown()) dentro do prompt do agente
tabulate>=0.9.0
